<%= markdown("This page was build using the `following: `") %>
<%= markdown("- Rails 5.1.6") %>
<%= markdown("- Ruby 2.4.0") %>
<%= markdown("- Start Bootstrap Resume Template") %>
<%= markdown("I also used the following `gems: `") %>
<%= markdown(
  "```ruby
  	gem 'bootstrap'
  	gem 'jquery-rails'
  	gem 'font-awesome-rails'
  	gem 'pygments'
  	gem 'redcarpet'

  ```
  "
) %>
